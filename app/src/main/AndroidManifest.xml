<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.projectsax.cookbook">

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:debuggable="true">

        <activity android:name=".activitypackage.MainMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activitypackage.ViewRecipeList" />
        <activity android:name=".activitypackage.SearchRecipe" />
        <activity android:name=".activitypackage.RecipeMaker" />
        <activity android:name=".activitypackage.NewIngredient" />
        <activity android:name=".activitypackage.NewInstruction" />
        <activity android:name=".activitypackage.ViewSelectedRecipe" />
    </application>

</manifest>